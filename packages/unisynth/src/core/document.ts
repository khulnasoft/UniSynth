/** Contains additional items which Unisynth patches on the Document. */
export interface UnisynthDocument extends Document {
  __q_context__?: [HTMLElement, Event, URL] | {};
}

export const _is_module = true;
