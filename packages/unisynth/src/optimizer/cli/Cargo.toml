[package]
name = "unisynth"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = "3.1.8"
unisynth-core = { path = "../core", features = ["fs", "parallel"] }
path-absolutize = "3.0.11"